apiVersion: v1
kind: Service
metadata:
  name: wms-gateway
  labels:
    app.kubernetes.io/name: wms-gateway
    app.kubernetes.io/part-of: wms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wms-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
