apiVersion: v1
kind: Service
metadata:
  name: wms-worker
  labels:
    app.kubernetes.io/name: wms-worker
    app.kubernetes.io/part-of: wms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wms-worker
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
