apiVersion: v1
kind: Service
metadata:
  name: wms-web
  labels:
    app.kubernetes.io/name: wms-web
    app.kubernetes.io/part-of: wms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wms-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
