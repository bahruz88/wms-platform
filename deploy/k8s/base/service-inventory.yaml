apiVersion: v1
kind: Service
metadata:
  name: wms-inventory
  labels:
    app.kubernetes.io/name: wms-inventory
    app.kubernetes.io/part-of: wms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wms-inventory
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
