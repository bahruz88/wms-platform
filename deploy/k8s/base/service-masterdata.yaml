apiVersion: v1
kind: Service
metadata:
  name: wms-masterdata
  labels:
    app.kubernetes.io/name: wms-masterdata
    app.kubernetes.io/part-of: wms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wms-masterdata
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
