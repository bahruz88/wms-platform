apiVersion: v1
kind: Service
metadata:
  name: wms-identity
  labels:
    app.kubernetes.io/name: wms-identity
    app.kubernetes.io/part-of: wms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wms-identity
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
