# wms_l10n

Tətbiqlərin lokalizasiyası: `az` (defolt), `en`, `ru`. ARB faylları `lib/l10n/`-dədir,
generasiya olunmuş `AppLocalizations` isə `lib/src/generated/` altında commit edilir.

Yenidən generasiya: `flutter gen-l10n` (bu qovluqdan) və ya kökdən `dart run melos run l10n`.
